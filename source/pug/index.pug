extends ./templates/template

block vars
    - var pageTitle = 'Остеопрактика'
    - var main = true
    - var pageId = 1

block content
    main.content
        +intro({
            mix: 'content__section',
        })
        +problems({
            mix: 'problems__section',
        })


    //-  +modal({
        mod: 'price-order'
        })
            +price-order({})
