include ./include

block vars
    - var pageTitle = 'Title'
    - var main = false

doctype html
html(lang="ru-RU")
    block head
        +head()

    body
        div.hidden(style='display: none;')
            include ./icons.pug
        block header
            +header({})
        block intro
        block content
        block footer
            +footer({})
        block scripts
            +scripts()
